package models.enums;

/**
 * Roles is an enumeration that represents different user roles in the application. The roles
 * include ADMIN, STUDENT, and PROFESSOR.
 */
public enum Roles {
  ADMIN,
  STUDENT,
  PROFESSOR
}
