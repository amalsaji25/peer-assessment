package models.enums;

/**
 * Status is an enumeration that represents different statuses for review tasks in the application.
 * The statuses include PENDING, COMPLETED, and ACTIVE.
 */
public enum Status {
  PENDING,
  COMPLETED,
  ACTIVE
}
